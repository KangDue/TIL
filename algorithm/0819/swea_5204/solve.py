import sys
sys.stdin = open('input.txt')

count = 0
def merge_sort(x):
    if len(x) == 1:
        return x
    else:
        left = x[:len(x)//2]
        right = x[len(x)//2:]
        return

def merge(a,b):


for t in range(1,int(input())+1):
    n = int(input())
    nums = list(map(int,input().split()))
